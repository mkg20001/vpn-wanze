affects:
 - '*'

modules:
  link:
    wanze/bin.sh: /usr/bin/wanze
    shared/scripts/update-bin.sh: /usr/bin/wanze-update
  pkg:
    - wireguard
lifecycle:
  install.post: |
    rm -v /etc/cjdroute.conf
